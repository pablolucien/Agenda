{
    "targets": [
        {
            "dirs": [
                "${ROOT_E}/m2_repository",
                "${LOCAL_M2_REPOSITORY_DIR}"
            ],
            "Exclude": "ebu/"
        }
    ]
}
